import React, { Component } from 'react';
import { connect } from 'react-redux'

class App extends Component {
	render() {
		return (
		<div className="App">
			But dat homepage doe
		</div>
		);
	}
}

export default connect(null,null)(App);
